import { Typography } from '@mui/material';
import { typography } from '@mui/system';
import * as React from 'react';

export default function Inicio(){
    return(
        <Typography>HOLA TOBIAS, BIENVENIDO A TU CUENTA</Typography>
    );
}