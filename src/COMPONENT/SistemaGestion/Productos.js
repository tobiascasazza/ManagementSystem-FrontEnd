import { Typography } from '@mui/material';
import { typography } from '@mui/system';
import * as React from 'react';

export default function Producto(){
    return(
        <Typography>ACA VA LA PAGINA DE PRODUCTOS</Typography>
    );
}